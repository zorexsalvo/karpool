version: '3.8'

services:
  web:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - .:/app
      - /app/.venv
      - sqlite_data:/app/data
    environment:
      - DEBUG=1
    command: python manage.py runserver 0.0.0.0:8000

volumes:
  sqlite_data: